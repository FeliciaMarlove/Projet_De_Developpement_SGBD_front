export class Client {
    idClient: number;
    nomClient: string;
    prenomClient: string;
  telephoneClient: string;
    // @ts-ignore
  dateNaissanceClient: Date;
    isActifClient: boolean;
}
