export class Display {
  art: string;
  des: string;
  qty: number;


  constructor(art: string, des: string, qty: number) {
    this.art = art;
    this.des = des;
    this.qty = qty;
  }
}
